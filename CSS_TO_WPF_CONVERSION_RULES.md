# CSS / HTML 转 WPF 控件高还原度规则

> 适用范围：将 HTML、CSS、SVG 和 Uiverse 等网页控件移植为 `CheemsControl` WPF 控件。
>
> 目标：在浏览器与 WPF 渲染机制不同的前提下，保证结构、尺寸、颜色、层级、状态、运动轨迹和交互逻辑尽可能达到 100% 语义还原。
>
> 并行目标：允许多个 AI 同时转换不同控件，但任何时刻同一个文件只能有一个写入者；控件私有实现可并行，公共注册与页面聚合必须串行集成。
>
> 本文是 `CONVENTIONS.md` 的专项补充；两者冲突时，以项目总规范和用户本次明确要求为准。
>
> 最后更新：2026-08-27

## 1. “100% 还原”的定义（F）

浏览器与 WPF 使用不同的布局、阴影、字体和抗锯齿引擎，因此“每一个屏幕像素绝对相同”通常不可实现。本项目所称高还原度必须同时满足：

| 编号 | 要求 |
|---|---|
| F1 | **结构一致**：原始 DOM 中承担不同职责的层，在 WPF 模板中有明确的一一对应；同一个 DOM 元素运动或变形时，不得擅自拆成两张互换的图。 |
| F2 | **静态视觉一致**：基准尺寸下的几何位置、尺寸、圆角、颜色、渐变方向、透明度、裁剪和层级与原版一致。 |
| F3 | **状态一致**：Normal、Hover、Pressed、Checked、Unchecked、Disabled、Focused 等原版存在的状态全部实现，组合状态按 CSS 选择器优先级生效。 |
| F4 | **运动一致**：动画的时长、延迟、循环、方向、关键帧、变换顺序、变换原点和缓动曲线均来自源码，不用“相近动画”代替。 |
| F5 | **交互一致**：点击区域、状态绑定、键盘操作、可逆切换和快速连续操作均正确，不得只做静态视觉。 |
| F6 | **无自创设计**：源码没有的渐变、描边、阴影、图形、颜色、弹性或状态不得添加；任何有意省略或替代必须由用户明确同意并登记。 |
| F7 | **运行可靠**：项目可编译、可启动、可反复交互，动画不泄漏、不刷异常、不造成布局递归或渲染线程崩溃。 |

允许的不可避免差异仅限渲染器产生的亚像素抗锯齿、阴影模糊核和字体栅格化差异。不得用“WPF 做不到”掩盖尺寸、颜色、结构或逻辑错误。

## 2. 转换前：冻结原版事实（S）

### 2.1 必须收集的输入

- 完整 HTML，而不是只截取可见节点。
- 完整 CSS，包括 `@keyframes`、媒体查询、伪元素、伪类和无障碍规则。
- 原版在浏览器中的静态截图或可运行页面。
- 控件的基准宽高、根字号、浏览器缩放和 DPI。
- 用户明确要求省略或改变的细节，例如“不要焦点外框”。

缺少某项时，应先从已有源码推导；不能推导且会影响结果时再询问用户。不得自行补设计。

### 2.2 先计算最终 CSS，再写 XAML

CSS 必须按真实级联顺序分析，特别注意：

- 选择器优先级、源码先后顺序和 `!important`。
- 后出现的同优先级规则会覆盖前面的规则。
- `:checked:hover` 等组合状态优先于单独的 `:hover` 或 `:checked`。
- 同一个伪元素可能先被隐藏，后又被更具体的规则重新显示。
- `animation` 会覆盖同一属性的普通声明或 `transition` 动画值。
- 继承值、自定义属性 `var(...)`、默认值和浏览器 User Agent 样式。

在实现前建立“最终样式表”，不要边看 CSS 边猜 XAML。

### 2.3 建立转换清单

每个控件至少记录以下事实：

| 项目 | 需要记录的内容 |
|---|---|
| 基准坐标系 | 根宽高、字号、DPI、`em/rem/%/px` 的最终像素值 |
| DOM 层级 | 元素、伪元素、前后遮挡关系、裁剪父级 |
| 静态样式 | 位置、尺寸、圆角、颜色、渐变、阴影、透明度 |
| 状态 | 触发条件、变化属性、选择器优先级 |
| 动画 | 名称、属性、时长、延迟、次数、方向、缓动、关键帧 |
| 变换 | transform 列表顺序、transform-origin、perspective |
| 响应式 | 每个断点中真正改变的属性 |
| 例外 | 经用户同意省略或改变的内容 |

并行开发时还必须在动手前记录：任务 ID、控件最终类名、负责 AI、私有文件清单、需要修改的公共文件、公共文件集成者及开工时基线提交。类名、资源键、源码文件名或搜索标识存在重名时，必须先完成命名仲裁，不能等写完后再解决。

## 3. 结构映射规则（M）

| 编号 | 规则 |
|---|---|
| M1 | 一个语义 DOM 节点对应一个主要 WPF 模板部件，名称统一使用 `Part*`。 |
| M2 | `::before`、`::after` 必须作为独立命名视觉元素实现，不得合并进背景图片或随意省略。 |
| M3 | DOM 顺序、定位上下文和堆叠顺序必须保留。XAML 后声明元素通常显示在上层；必要时显式使用 `Panel.ZIndex`。 |
| M4 | `overflow: hidden` 必须映射为准确的 `Clip` 或 `ClipToBounds`。圆角裁剪优先使用 `RectangleGeometry`，球体裁剪使用 `EllipseGeometry`。 |
| M5 | `position: absolute` 应在固定基准坐标系的 `Canvas` 中表达；不要依赖会重新分配空间的 `StackPanel` 或自动网格布局。 |
| M6 | 同一元素从太阳变成月亮、从云变成陨石坑时，必须让同一视觉层连续插值；禁止用两套不相关视觉直接切换，除非原版本身就是交叉淡入淡出。 |
| M7 | 原版点击目标若是隐藏的 `<input>` 加 `<label>`，WPF 通常映射为 `ToggleButton`、`CheckBox` 或其派生控件；模板负责视觉，控件本身负责状态和可访问性。 |
| M8 | 纯装饰元素必须 `IsHitTestVisible="False"`，不能遮挡控件点击。 |
| M9 | SVG 的 `viewBox`、mask、clipPath 和 transform 必须保留同一坐标系；优先用 `Path`、`Geometry`、`OpacityMask` 和 `Viewbox`，禁止先栅格化再冒充矢量还原。 |
| M10 | `transform-style: preserve-3d` 与 `translateZ(...)` 即使没有透视缩放，也决定子元素在父背景之前或之后的绘制层级。WPF 2D 替代时必须显式重排为“负 Z 子层 → 父背景/阴影 → 普通子层”，不得按 HTML 文本顺序简单平铺。 |
| M11 | 父元素的 `border-radius` 不等于 `overflow: hidden`。源码未设置裁剪时，负定位的子元素、耳朵、尾巴等必须允许越过父轮廓；不得为了修复穿模给整个父层擅加 `Clip`。 |

## 4. 尺寸与布局规则（L）

### 4.1 单位换算

- 先确定根字号，再将 `em` 转为精确设备无关像素。例如根字号 `18px` 时：`0.3em = 5.4`、`2.4em = 43.2`。
- `%` 必须确认相对的是父级宽度、高度、渐变坐标还是变换自身尺寸。
- WPF 的 1 DIP 在 100% 缩放下对应 1 像素；高 DPI 验收时需记录缩放比例。
- 不要为“数字好看”把 `5.4` 四舍五入成 `5`，否则多层误差会累积。

### 4.2 基准画布与任意尺寸

- 先按原版基准尺寸建立固定坐标模板，再用 `Viewbox Stretch="Uniform"` 整体缩放。
- 对外控件应允许设置 `Width/Height`，内部比例必须保持原版。
- 必须验证至少原始尺寸、一个较小尺寸和一个非整数缩放尺寸。
- CSS 媒体查询按浏览器视口生效，不等于按控件宽度生效。只有产品需求明确要求时，才将断点改成控件尺寸逻辑。

### 4.3 几何误差标准

- 基准尺寸下，关键边界、中心和运动终点与原版误差不超过 1 DIP。
- 运动元素的起点、终点和最大越界量必须由 CSS 数值计算，不能目测。
- 圆角、裁剪和阴影不能导致视觉内容溢出原版边界。

### 4.4 CSS 椭圆圆角与 `clip-path`

- CSS `border-radius: h1 h2 h3 h4 / v1 v2 v3 v4` 的水平、垂直半径是两组独立数据，不能用 WPF `CornerRadius`、圆角矩形或目测贝塞尔曲线代替。
- 百分比水平半径相对元素宽度，垂直半径相对元素高度；相邻半径总和超出边长时，必须先按 CSS 规范的统一缩放因子归一化。
- 高还原几何应使用 `PathGeometry` 的椭圆 `ArcSegment`，并记录每个角的 `(rx, ry)`。任一轴半径为零时，该角按方角处理。
- `clip-path: polygon(...)` 与 `border-radius` 同时存在时，最终轮廓是两者的交集。不能只实现 polygon，也不能为“圆润”自行把 polygon 顶点改成曲线。
- 几何根部是否出现直角，必须先判断它在原版中是被父背景遮挡、被裁剪，还是本来可见；不要通过随意加圆角掩盖层级错误。

## 5. 颜色、渐变、阴影与裁剪（V）

| 编号 | 规则 |
|---|---|
| V1 | 平面色必须保持平面色。源码是 `#ffd700` 就不能改成黄色渐变。 |
| V2 | 发光或阴影必须放在主体后方的独立层，不能覆盖主体后造成“主体看起来像渐变”。 |
| V3 | 颜色、Alpha、渐变色标、色标位置和方向必须逐值转换。`rgba(...,.4)` 不能凭感觉改成其他透明度。 |
| V4 | CSS `linear-gradient(to right, ...)` 对应 WPF `StartPoint="0,0" EndPoint="1,0"`；其他方向按向量换算。 |
| V5 | CSS 多重 `box-shadow` 必须逐层实现；WPF 一个元素只能设置一个 `Effect` 时，应使用多个同尺寸背景层模拟，且保留声明顺序。 |
| V6 | `inset` 阴影不能简单替换成外阴影。需要用裁剪、渐变遮罩、几何差集或内部覆盖层实现。 |
| V7 | 阴影主体、扩散区和模糊区要分清。用缩放的径向渐变模拟时，实色区半径、模糊半径和最终 Alpha 必须从 CSS 的 blur/spread 计算。 |
| V8 | 遮挡层必须受与原版相同的父级裁剪约束；月相、液体、波纹等不得越过球体或容器边缘。 |
| V9 | 不允许因 WPF `DropShadowEffect` 的默认行为产生源码没有的彩边、暗边或内部染色。 |
| V10 | 所有颜色仍须遵守 `CONVENTIONS.md` 的资源三层规则；代码端读取资源键使用 `CheemsKeys`。 |
| V11 | `hsl()` / `hsla()` 必须按 sRGB 公式精确换算并按通道统一舍入，不能从截图取色，也不能复用“看起来接近”的已有色。例如饱和度变化会同时改变亮部与暗部，误差会在相邻色层接缝处被放大。 |
| V12 | 零模糊、零扩散的 `inset box-shadow` 优先用几何差集实现：设元素轮廓为 `S`，偏移副本为 `T(S, dx, dy)`，可见阴影区域为 `S − T(S)`；多重阴影按 CSS 列表从后向前绘制，使列表第一项位于最上层。 |
| V13 | 多重 inset shadow 不能合并成手绘色带。主体、每个偏移副本必须使用同一份原始轮廓 Geometry，否则圆角处与相邻 DOM 部件连接处会产生形状和颜色断层。 |
| V14 | 透明色仍需区分语义：渐变中的透明色若只影响 Alpha，可使用公共透明资源；若插值过程依赖隐藏 RGB 通道，则必须保留源码透明色的 RGB，避免渐变中段偏色。 |
| V15 | CSS `box-shadow` 的 blur 与 WPF `DropShadowEffect.BlurRadius` 不是同一个模糊核，数值相同只可作为初始值，不能视为已经还原；最终必须按原尺寸截图校准边缘衰减、峰值浓度和可见范围。 |
| V16 | `DropShadowEffect` 的阴影透明度默认使用“不透明 RGB 色 + Effect.Opacity”表达，避免仅依赖 `Color.Alpha` 在不同渲染路径中出现发黑或强度失真；同一层不得同时在 Color.Alpha 和 Effect.Opacity 重复乘透明度。 |
| V17 | 为模拟多重外阴影创建的辅助层只能承担该层阴影。`DropShadowEffect` 会连同源元素一起渲染，多个带实体背景的副本会重复加深主体边缘；必须由最上层主体完全覆盖副本实体，或使用仅含轮廓/遮罩的阴影源。 |
| V18 | 阴影不能靠增大控件本体或移动主体来“腾空间”。应建立较大的外层画布，把原始本体放在固定坐标，再按每侧最大 `blur + spread + |offset|` 预留保守边距；同时检查 Viewbox、ScrollViewer、Border 和 Popup 的实际裁剪链。 |
| V19 | CSS 拟态阴影依赖宿主背景色。验收时必须同时记录原版背景和 Demo 实际背景；背景不同导致的对比度变化不能通过随意加深阴影补偿。若控件只在特定背景上成立，应明确这一设计约束。 |

### 5.1 CSS 阴影到 WPF 的固定换算流程

每一个 `box-shadow` 在动手写 XAML 前都要拆成一行数据：

| 层 | inset | dx | dy | blur | spread | RGB | Alpha | 绘制顺序 |
|---|---:|---:|---:|---:|---:|---|---:|---:|
| 示例 | 否 | 6 | 6 | 12 | 0 | `#B8BEC9` | 1 | 1 |

实施时遵守以下顺序：

1. **先定方向和位移。** CSS `(dx,dy)` 转为 WPF 时，`ShadowDepth = sqrt(dx² + dy²)`，`Direction = normalizeDegrees(atan2(-dy, dx))`。例如 `(6,6)` 对应约 `Depth=8.485, Direction=315°`，`(-6,-6)` 对应 `135°`。不得用目测角度。
2. **再分离 spread 与 blur。** CSS spread 先扩张或收缩阴影源几何，blur 只处理扩张后轮廓的软化。WPF 没有 spread 属性，禁止把 spread 直接累加到 `BlurRadius`；应使用放大后的同轮廓 Geometry、额外 Border/Path 或蒙版表达。
3. **Alpha 只放在一个位置。** 对 `rgba(r,g,b,a)`，默认资源色保存为不透明 `rgb(r,g,b)`，把 `a` 写入 `DropShadowEffect.Opacity`。若经实机验证选用 `Color.Alpha`，则 Effect.Opacity 必须为 1，并在转换记录中注明；禁止两边同时应用。
4. **一个 CSS shadow 对应一个视觉层。** 多重阴影按 CSS 列表从后向前绘制。每层分别保留自己的偏移、模糊、扩散、颜色和透明度，不能把两层平均成一层，也不能让多个实体副本暴露在主体边缘。
5. **外阴影、内阴影、发光分开实现。** 外阴影可使用 `DropShadowEffect` 或模糊轮廓；inset 使用几何差集、裁剪或内部渐变遮罩；`0 0 blur color` 发光使用居中的独立模糊层。三者不得互相替代。
6. **先静态校准，再接动画。** 先在 0%、100% 状态分别确认阴影，再实现状态过渡。Effect/Brush 属于 Freezable；需要连续改变阴影时，使用每实例克隆或两个独立阴影层做 Opacity 交叉动画，禁止修改共享资源。
7. **预留外溢区域。** 记录四侧实际可见边界，扩大模板外层画布并保持本体基准坐标不变。阴影层不得被本体的 `overflow:hidden` 等价裁剪误伤；只有原 CSS 的裁剪祖先才能裁切它。

### 5.2 阴影视觉校准标准

阴影不能只靠“看起来差不多”验收。浏览器与 WPF 必须在相同尺寸、DPI 和背景下截取静态帧，并至少检查：

- **主体边界：** 阴影不能把圆、拨块或药丸边缘染黑、增粗或变成渐变。
- **峰值位置：** 最暗/最亮区域相对主体的方向和距离一致，不能把右下阴影画成正下方阴影。
- **衰减宽度：** 从主体边缘到不可见处的距离一致；若范围过大，先调整 WPF 模糊半径或阴影源尺寸，不能只降 Alpha 掩盖。
- **峰值浓度：** 范围正确后再调 Opacity；若出现实黑块，优先检查 Color.Alpha、Effect.Opacity 和实体副本叠加，而不是继续缩小 BlurRadius。
- **多层关系：** 近层的硬边、远层的软边和高光层必须能分别辨认，不能糊成一圈统一光晕。
- **裁剪：** 在控件原尺寸、页面容器和非整数缩放下分别确认四侧阴影完整，没有只在某一边被截断。
- **背景依赖：** 至少在原版背景与 Demo 背景各检查一次；浅色拟态控件尤其要确认白色高光和灰色暗影没有消失或过强。

## 6. 状态与 CSS 级联映射（C）

### 6.1 状态矩阵必须先写后实现

示例：

| 状态 | Track | Thumb | 伪元素 | 动画 |
|---|---|---|---|---|
| Normal + Unchecked | 白天渐变 | 太阳 | 云朵 | sunPulse |
| Hover + Unchecked | 白天 Hover 渐变 | 不变 | 云朵 | sunPulse |
| Normal + Checked | 夜晚渐变 | 月亮 | 陨石坑 | moonPhase + twinkle |
| Hover + Checked | 夜晚 Hover 渐变 | 月亮 | 陨石坑 | moonPhase + twinkle |

没有状态矩阵，不得直接开始写 Trigger。

### 6.2 WPF 映射原则

- 简单离散状态用 `Trigger` / `MultiTrigger`。
- 需要连续插值、可中断反向或涉及多属性协同时，可在控件代码中使用统一进度驱动。
- `IsChecked` 改变后必须能再次改变回来；不得用只运行一次、只覆盖单向状态的 Storyboard。
- `IsThreeState`、数据绑定和 `IsChecked="{Binding ..., Mode=TwoWay}"` 必须正确工作。
- 快速连续点击时，新动画从当前视觉值继续，不得跳回旧起点。
- Focused 状态原则上应保留；只有用户明确要求省略时才移除，并在转换清单中登记。

## 7. 动画与变换规则（A）

### 7.1 动画参数逐项翻译

每个动画必须核对：

1. 动画作用的真实元素和属性。
2. `duration`。
3. `delay`，包括负延迟。
4. `iteration-count`。
5. `direction`：normal、reverse、alternate。
6. `fill-mode`。
7. 每个关键帧的百分比和值。
8. `animation-timing-function` 与 `transition-timing-function`。
9. 多动画同时作用时的覆盖关系。

禁止把 `ease`、`linear`、自定义 cubic-bezier 和弹簧动画混为一谈。

### 7.2 cubic-bezier

- CSS `cubic-bezier(x1,y1,x2,y2)` 的输入是时间轴 `x`，输出才是属性进度 `y`。
- 代码驱动时必须先求解贝塞尔曲线的 `x(t)=time`，再取 `y(t)`；不能直接把当前时间当曲线参数。
- `y1 < 0` 或 `y2 > 1` 产生的过冲必须保留，这通常正是 Q 弹感的来源。
- WPF `KeySpline` 无法准确表达或协调目标动画时，使用统一逐帧求值器，不得换成主观选择的 `BackEase`。

### 7.3 transform 顺序

- CSS transform 列表的顺序会改变最终矩阵，必须逐项保留。
- CSS 以列向量理解时，`transform: rotate(...) translate(...)` 对点的实际作用是先 `translate`、后 `rotate`；WPF `TransformGroup.Children` 按自身组合规则执行，不能照抄文本顺序。实现前必须用一个非原点测试点验证最终矩阵，而不是凭 API 名称猜顺序。
- 必须记录 `transform-origin`。
- `translateX(...) translateZ(...) rotateY(...)` 不能简化成单纯左右移动。
- `transform-origin` 百分比基于元素自身边界。子元素嵌套动画必须逐级保留父、子原点，不能把多个旋转合并到根节点，否则重叠量和连接轮廓会随动画漂移。
- WPF 2D 模板模拟 `rotateY` 时，至少应按透视投影让水平尺度随角度收缩并翻面；要求严格 3D 时使用 `Viewport3D` 或矩阵投影。
- 运动过程中球体变窄、恢复和翻面属于原版变换结果，不得删除。

### 7.4 循环动画生命周期

- 遵守 `CONVENTIONS.md` C6：`RepeatBehavior="Forever"` 不得放在属性 Trigger 的 EnterActions/ExitActions 中。
- 代码订阅 `CompositionTarget.Rendering` 时，只能在 `Loaded` 后订阅，在 `Unloaded` 时解除订阅。
- 同一实例不能重复订阅；模板重建后不能保留旧部件引用。
- 页面移除后 CPU 占用必须恢复，控件不得因静态事件形成内存泄漏。
- 状态动画与环境循环动画作用于同一属性时，必须建立明确的合成规则，不能互相覆盖或抢占。

## 8. WPF 特有陷阱（W）

| 编号 | 规则 |
|---|---|
| W1 | XAML 模板中的 `Brush`、`Transform`、`Geometry` 等 `Freezable` 可能被 WPF 自动冻结。代码需要逐帧修改时，必须 `Clone()` / `CloneCurrentValue()` 后重新赋给控件实例。 |
| W2 | 不得修改共享资源实例来驱动单个控件动画，否则多个实例会串色、串状态或触发只读异常。 |
| W3 | 代码驱动的画刷、变换和几何必须是每个控件实例独享。 |
| W4 | `ResourceDictionary.Source` 使用项目规定的程序集绝对 pack URI，避免样式加载时相对路径解析失败。 |
| W5 | `StaticResource` 在加载时解析，`DynamicResource` 在运行时解析；选择错误会造成启动异常、无法换肤或 Freezable 冻结问题。 |
| W6 | `OnApplyTemplate` 可能执行多次。每次必须重新获取全部 `Part*`、克隆可写对象并同步当前状态。 |
| W7 | `OnChecked/OnUnchecked` 可能早于模板创建；代码必须允许部件为空，并在 `OnApplyTemplate` 后恢复正确视觉状态。 |
| W8 | 不要在逐帧回调中修改会触发布局测量的属性，除非确有需要。优先使用 RenderTransform、Opacity 和画刷值，避免每帧 Measure/Arrange。 |
| W9 | 全局异常处理器不能成为“控件看似没崩”的理由。验收必须检查异常日志增量，任何逐帧异常都算失败。 |
| W10 | Demo 中同时放置多个实例，确保资源、变换、动画时钟和状态完全隔离。 |
| W11 | 两个独立 Path 在静态状态重叠正常，分别旋转或缩放后仍可能因 WPF 的亚像素抗锯齿出现 1px 背景缝。先确认坐标、重叠量和矩阵均正确；若只剩渲染器接缝，可在最小连接区重绘原部件已有色层作为 overdraw。补偿层必须使用原 Geometry/Brush、跟随正确父级运动、避开其他子元素，禁止用新颜色描边或整体放大掩盖。 |
| W12 | `CombinedGeometry`、带 Transform 的 Geometry 等适合表达零模糊 inset shadow，但若代码需要修改，仍属于 `Freezable`；静态资源可共享，逐帧变形的实例必须克隆。 |

## 9. 可访问性与用户偏好（E）

- 原版 `role`、checked 语义和键盘切换能力应通过正确的 WPF 控件基类保留。
- 装饰层不参与焦点和命中测试。
- `IsEnabled="False"` 必须阻止交互，并提供与源码一致或项目规范要求的禁用视觉。
- CSS `prefers-reduced-motion` 映射到 `SystemParameters.ClientAreaAnimation`；关闭动画时直接显示最终状态。
- CSS `forced-colors` 映射到 `SystemParameters.HighContrast` 和 `SystemColors`。
- 焦点外框、Automation Name 和键盘 Space 切换需验收；用户明确要求删除焦点框时，仅删除视觉，不破坏键盘与状态语义。

## 10. 固定实施流程（P）

### P0：并行任务开工协议

1. 开工前读取 `git status --short`，将已有修改视为他人工作，不得清理、回退、暂存或顺手修复。
2. 优先为每个控件使用独立 Git branch + worktree；无法隔离时，必须执行第 16 节的“共享工作区模式”。
3. 每个任务先声明写入集。控件类、控件样式和对应源码文本属于该控件的私有文件；`Generic.xaml`、基础资源、Demo 聚合页和搜索 ViewModel 属于公共文件。
4. 同一个文件同一时刻只能由一个 AI 持有写权限。没有取得公共文件所有权的 AI 只能提交集成清单，不得修改公共文件。
5. 读取过的公共文件不是永久快照。真正写入前必须重新读取并核对文件指纹；文件已变化时停止套用旧补丁，交回公共集成者重新合并。
6. 不允许为了减少冲突而省略 Demo、搜索词、资源注册或运行验收；这些工作只是延后到串行集成阶段，不是取消。

### P1：原版基线

1. 保存完整 HTML/CSS/SVG。
2. 在浏览器按基准尺寸运行。
3. 截取所有静态状态和关键动画时刻。
4. 记录用户允许的例外。

### P2：分析

1. 计算所有相对单位。
2. 展开 CSS 级联得到最终样式。
3. 建立 DOM → WPF 部件映射。
4. 建立状态矩阵和动画时间表。
5. 对非对称 `border-radius`、`clip-path`、多重 shadow 和 transform 建立数值表；禁止边写 XAML 边反推参数。
6. 明确每个子元素是在父背景后、父背景前还是独立堆叠上下文中，尤其标出 `translateZ(-1px)` 等仅承担层级职责的变换。

### P3：静态模板

1. 只实现默认静态状态。
2. 校准尺寸、层级、裁剪、颜色、渐变和阴影。
3. 静态状态未对齐前，不开始写复杂动画。
4. 相邻部件使用同色衔接时，放大到 400% 检查轮廓和色层接缝；静态接缝未消除前不得依赖动画“遮过去”。

### P4：状态

1. 实现 Hover、Checked、Disabled、Focused 等状态。
2. 验证组合选择器的覆盖关系。
3. 验证绑定和双向切换。

### P5：动画

1. 按源码逐个加入动画。
2. 先验证位移与变形，再验证循环环境效果。
3. 验证中途反向、快速点击和多实例。

### P5.1：Demo 展示组织

1. **一个对外控件只使用一个演示容器**，可选大容器 `ShowcaseControl` 或小容器 `CompactShowcaseControl`；不得按“基本用法、尺寸、状态”等为同一控件重复创建容器。
2. 按钮、开关、RadioButton/评分等简单且单实例即可说明交互的控件，使用无标题、无边框、无背景的 `CompactShowcaseControl`。容器只横向排列“控件 + 最右侧源码按钮”，页面用 `WrapPanel` 允许一行排列多个小容器。
3. Loader 与其他仅需单实例说明效果的控件统一使用小容器 `CompactShowcaseControl`；即使动画结构复杂，也不因实现复杂度改用大容器。只有确需标题、展示台背景或必要变体时才使用 `ShowcaseControl`。
4. 简单控件默认只保留一个最能代表原版的实例。确有展示价值的尺寸、状态或属性变体，只能集中排列在同一个大容器内并共用一份源码预览。
5. 变体没有体现新的视觉、交互或公开属性时不展示；同一控件的实例数量以说明必要差异的最小数量为准。
6. 大、小演示容器的源码按钮统一使用极简文本样式：无背景、无边框、无阴影、无悬停底色和焦点框，仅保留文字、预览与复制交互。

### P6：视觉对比

1. 浏览器和 WPF 使用相同基准尺寸、背景和缩放。
2. 对比默认、Hover、Checked、Disabled。
3. 对比动画 0%、25%、50%、75%、100% 时刻。
4. 对截图做叠加或差分，逐个解释差异；存在未解释差异不得交付。
5. 对有独立父子旋转或重叠部件的动画，额外检查每段中点和最大角度帧；关键帧端点无缝不代表插值途中无缝。
6. 至少以 100% 与 400% 两种倍率检查细线、抗锯齿暗边、背景漏缝和一像素溢出。400% 只用于发现问题，最终仍以原尺寸视觉为准。
7. 含阴影的控件必须额外截取“仅静态默认态”和“静态选中态”，分别比较主体边界、阴影峰值位置、衰减范围和峰值浓度；不能用动画中的模糊帧代替静态验收。
8. 多重阴影应临时逐层显示一次，确认每层单独正确后再合成；若合成后明显变黑或边缘增粗，优先排查实体副本重复绘制与 Alpha 重复相乘。

### P7：运行验收

1. 构建解决方案，要求 0 错误；新代码原则上 0 警告。
2. 实际启动 Demo，不能只依赖 XAML 编译通过。
3. 执行至少两轮 `Unchecked → Checked → Unchecked`。
4. 快速连续点击至少 10 次。
5. 分别测试多个尺寸和多个同时运行的实例。
6. 观察异常日志、CPU 和卸载后的动画订阅。

## 11. 强制验收表（Q）

转换完成前必须逐项打勾：

### 源码与结构

- [ ] 已读取完整 HTML、CSS、SVG 和全部关键帧。
- [ ] 已解析选择器优先级与后声明覆盖。
- [ ] DOM、伪元素和 WPF 部件存在一一对应表。
- [ ] 没有添加源码不存在的设计。
- [ ] 所有经用户同意的例外已经登记。

### 静态视觉

- [ ] 基准宽高和所有相对单位已精确换算。
- [ ] 平面色、HSL/RGB 换算、渐变、Alpha、圆角和层级与原版一致。
- [ ] 非对称椭圆圆角按水平/垂直两组半径转换，没有用普通 CornerRadius 或目测曲线代替。
- [ ] 阴影的内外、方向、扩散和模糊已核对。
- [ ] 每层 CSS 阴影已有 dx/dy/blur/spread/RGB/Alpha 数值表，WPF Direction 与 ShadowDepth 由公式换算而非目测。
- [ ] DropShadowEffect 没有同时使用 Color.Alpha 与 Effect.Opacity 重复表达透明度。
- [ ] 多重外阴影的辅助实体层被主体完整覆盖，没有重复加深轮廓。
- [ ] 阴影四侧外溢空间充足，在 Viewbox、ScrollViewer 和非整数缩放下均未裁切。
- [ ] 阴影已在原版背景和 Demo 实际背景上分别验收。
- [ ] 多重 inset shadow 的几何、偏移和前后顺序已核对。
- [ ] 所有需要裁剪的内容不会溢出。
- [ ] 没有添加源码不存在的父级裁剪；允许越界的子元素仍可见。
- [ ] 相邻或重叠部件在 100% / 400% 下没有背景缝和异常描边。
- [ ] 原尺寸、小尺寸和非整数缩放尺寸均正确。

### 状态与动画

- [ ] 所有状态及组合状态已覆盖。
- [ ] 动画时长、延迟、循环、方向和关键帧一致。
- [ ] cubic-bezier 按 CSS 数学定义求值。
- [ ] transform 的最终矩阵、WPF 组合顺序和 origin 一致，并用非原点测试点验证。
- [ ] `translateZ` 对应的前后绘制层级已经保留。
- [ ] 动画中途的大小变化、翻转和过冲均保留。
- [ ] 父子元素独立运动时，关键帧端点、段中点和最大变换帧均无接缝。
- [ ] 切换可双向往返，快速点击不会卡死或跳帧。
- [ ] Reduced Motion 和 High Contrast 行为正确。

### WPF 可靠性

- [ ] 可变 Freezable 已为每个实例克隆。
- [ ] `OnApplyTemplate` 重入安全，模板为空时不抛异常。
- [ ] 循环动画在 Unloaded 后停止或解除订阅。
- [ ] 多实例之间不共享可变动画对象。
- [ ] 每个对外控件只有一个演示容器；简单控件使用透明小容器且仅一个实例，必要变体集中在同一大容器，不必要变体未展示。
- [ ] Demo 实际启动并完成交互测试。
- [ ] 运行期间异常日志增量为 0。
- [ ] 构建结果为 0 错误、0 新警告。

### 并行协作

- [ ] 已登记任务 ID、控件名、负责 AI、基线提交和文件写入集。
- [ ] 控件类名、资源键、源码文件名、搜索标识均未与其他并行任务重名。
- [ ] 本任务只修改自己拥有的私有文件；公共文件由指定集成者串行修改。
- [ ] 写公共文件前已重新读取内容并确认它没有脱离当前基线。
- [ ] 没有覆盖、格式化、删除或提交其他 AI 的未完成修改。
- [ ] 已提供完整集成清单，且公共注册、Demo、搜索和资源项均已落地。
- [ ] 所有并行任务合入后重新完成全量构建、启动和异常日志验收。

## 12. 禁止做法（X）

- 只看截图，不读完整 CSS。
- 用户指出一处就只修一处，而不重新审计整份源码逻辑。
- 用个人审美“优化”原版。
- 把纯色改成渐变，或把亮面改成暗色阴影。
- 用两个静态图层切换代替一个 DOM 元素的连续变形。
- 用默认 WPF 缓动冒充源码 cubic-bezier。
- 忽略 transform 顺序、transform-origin 或 3D 透视。
- 按 CSS transform 的书写顺序直接排列 WPF TransformGroup，而不验证最终矩阵。
- 用 WPF `CornerRadius` 或随手画的三次贝塞尔代替 CSS 非对称椭圆圆角。
- 把多重零模糊 inset shadow 画成几条主观色带，或颠倒 shadow 列表的覆盖顺序。
- 忽略 `translateZ(-1px)` 的层级含义，把应位于父背景后的肢体、尾巴画到最上层。
- 为解决溢出直接缩小内容，而不是恢复正确裁剪关系。
- 为解决穿模给源码没有 `overflow:hidden` 的父层添加整体裁剪。
- 为遮住动态接缝给整个部件加描边、放大轮廓或使用源码不存在的过渡色。
- 只测试第一次点击，不测试返回和快速连续点击。
- 只运行 `dotnet build`，不启动 Demo。
- 捕获并吞掉逐帧异常后继续交付。
- 修改冻结或共享的 Brush/Transform/Geometry。
- 为同一个控件的基本用法、尺寸或状态重复建立多个演示容器。
- 用带标题、边框和展示台背景的大容器包装只需单实例即可说明的简单按钮、开关或单选控件。
- 多个 AI 在同一工作区同时修改 `Generic.xaml`、`Colors.xaml`、`Brushes.xaml`、`CheemsKeys.cs`、类别页面或搜索 ViewModel。
- 依据开工时读取的旧内容生成整文件替换，覆盖后来出现的并行修改。
- 为解决合并冲突运行 `git reset --hard`、`git checkout --`、`git clean`、全局 `stash` 或覆盖式复制。
- 使用全文件格式化、属性重排或无关重构扩大差异范围。
- 将其他 AI 的未完成文件一起 `git add`、提交或删除。
- 未经命名仲裁创建同名控件、资源键、源码文件或搜索属性。

## 13. 每个控件的转换记录模板

复制以下内容到控件实现说明或任务记录：

```md
# CheemsXxx 转换记录

## 并行协作
- 任务 ID：
- 负责 AI：
- 基线提交：
- 私有文件（本任务独占）：
- 公共文件集成者：
- 需要串行集成的公共文件：
- 依赖的其他并行任务：无 / 任务 ID

## 来源
- 作者/页面：
- HTML/CSS/SVG 文件：
- 基准尺寸与字号：
- 参考截图：

## 用户批准的差异
- 无 / 具体差异：

## DOM → WPF
| HTML/CSS | WPF Part | 说明 |
|---|---|---|
| .root | PartRoot | |
| .element | PartElement | |
| .element::before | PartElementBefore | |

## 几何与层级
| 对象 | 基准尺寸 | 圆角 / clip-path | 阴影 / 遮罩 | 绘制层级 |
|---|---:|---|---|---|
| | | | | |

## 阴影参数
| 层 | 对象 | inset | dx | dy | blur | spread | RGB | Alpha | WPF 实现 | 验收背景 |
|---|---|---:|---:|---:|---:|---:|---|---:|---|---|
| | | | | | | | | | | |

## 状态矩阵
| 状态 | 变化属性 | 动画 |
|---|---|---|
| Normal | | |
| Hover | | |
| Checked | | |

## 动画参数
| 动画 | 时长 | 延迟 | 缓动 | 循环/方向 | 作用属性 |
|---|---:|---:|---|---|---|
| | | | | | |

## 验收
- [ ] 静态截图对齐
- [ ] 阴影逐层与合成截图对齐
- [ ] 原版背景与 Demo 背景均已检查
- [ ] 关键动画帧对齐
- [ ] Checked/Unchecked 往返
- [ ] 快速连续操作
- [ ] 多尺寸/多实例
- [ ] 0 运行时异常
- [ ] 0 构建错误/新警告
```

## 14. 本次昼夜开关沉淀的关键结论

1. HTML 中只有一个 `.slider-inner`，因此 WPF 也应让一个球体从太阳连续变为月亮，而不是太阳/月亮两层互换。
2. 太阳主体是纯色；光晕属于独立 `box-shadow`，不能画进主体渐变。
3. 云朵和陨石坑来自同两个伪元素，应连续改变尺寸、位置和颜色。
4. `rotateY(180deg)` 会让球体在途中水平收缩并翻面；只做左右平移会丢失原版 Q 弹运动。
5. 月相亮面必须裁剪在球体圆形内部。
6. CSS 的弹性来自 `cubic-bezier(.68,-.55,.265,1.55)` 的过冲，不能用随意选择的 WPF Ease 替换。
7. WPF 会冻结模板中的 Transform；逐帧修改前必须克隆，否则会在运行时持续抛出只读异常。
8. “能编译”不等于“能运行”。最终必须启动 Demo、自动往返切换并确认异常日志没有增长。

## 15. 本次仓鼠跑轮与近期几何控件沉淀的关键结论

1. “结构相似”不等于结构一致。头部必须继续作为身体的子层，父身体旋转与头部点头要分别保留；把两者改成根画布上的兄弟节点会破坏原始旋转中心和稳定重叠量。
2. CSS 非对称 `border-radius` 是四组椭圆弧。使用普通圆角矩形或主观贝塞尔曲线，会同时改变头身轮廓、阴影边界和连接面积。
3. 零模糊 inset shadow 不是渐变。用 `S − T(S, dx, dy)` 的几何差集可以让主体与每层阴影共享完全相同的边界，避免手绘色带在圆角和连接处断开。
4. CSS 多重 shadow 的第一项位于视觉最上层；实现时应先画列表末项，再依次向前覆盖。
5. `translateZ(-1px)` 在该类 2D 控件里主要承担绘制层级职责。后侧肢体和尾巴应先画，再画身体背景；前侧肢体最后画。
6. `rotate(...) translate(...)` 不能按文字顺序直接塞进 `TransformGroup`。必须根据两端矩阵规则得到相同的点变换结果，否则整个仓鼠会沿错误轨迹摆动。
7. HSL 的一两个通道误差在孤立图形上可能不明显，但相邻浅色、橙色和粉色层会把误差放大成“接缝”；颜色必须数学换算，不能截图取近似值。
8. WPF 对两个独立动画 Path 分别抗锯齿时，即便它们在数学上重叠，也可能在某些角度露出 1px 背景缝。先排除坐标与矩阵错误；确认只是渲染器差异后，仅在最小连接区重绘原有色层，不改变外轮廓。
9. 修复肢体穿模时应恢复原始 `clip-path`、父子遮挡和 Z 层级；给整个仓鼠或轮子增加源码不存在的裁剪，会造成耳朵、尾巴或极值动画被切掉。
10. 几何动画必须同时验收静态帧、关键帧段中点和最大旋转帧。头身在 0% 看似连接，并不能证明 12.5% 的独立旋转仍无缝。

## 16. 多 AI 并行添加控件规范（PAR）

### 16.1 文件分区与所有权

并行开发采用“私有文件并行、公共文件串行”的原则。

| 分区 | 典型文件 | 写入规则 |
|---|---|---|
| 控件私有 | `Controls/CheemsXxx.cs`、`Themes/Controls/CheemsXxx.xaml`、`Sources/<Category>/Xxx.xaml.txt`、该控件专属图片 | 一个任务独占，可与其他控件并行开发。 |
| 公共资源 | `Themes/Basic/Colors.xaml`、`Brushes.xaml`、`Fonts.xaml`、`CheemsKeys.cs` | 只允许公共集成者串行追加；工作 AI 在集成清单中给出精确资源键和值。 |
| 公共注册 | `Themes/Generic.xaml`、项目文件、程序集级元数据 | 只允许公共集成者修改；不得由每个控件任务各自抢写。 |
| Demo 聚合 | `ButtonsPage.xaml`、`LoadersPage.xaml`、`InputsPage.xaml` 及对应 ViewModel | 只允许该类别的页面集成者串行修改。 |
| 应用外壳 | `App.xaml`、`MainWindow.*`、`MainViewModel.cs`、共享 Infrastructure | 默认不属于控件任务；只有用户明确要求应用级改动时才分配所有权。 |
| 规范文档 | `CONVENTIONS.md`、本文 | 默认只读；确需更新时单独分配文档所有权。 |

“文件所有权”是写权限，不是业务归属。任何 AI 都可以读取公共文件，但没有所有权就不得写入。所有权应由主任务、协调者或用户统一分配，不通过多个 AI 各写一个锁文件来竞争；锁文件本身也会成为冲突源。

### 16.2 推荐模式：独立 branch + worktree

1. 每个控件使用独立分支和独立 worktree，分支名应能唯一对应任务与控件。
2. 每个任务优先产生两个逻辑提交：
   - **控件提交**：只含私有文件，可直接独立审查和合入。
   - **集成提交**：只含公共资源、注册、Demo 和搜索项，由集成者统一创建或重做。
3. 集成者先合入所有控件提交，再根据各任务的集成清单一次性整理公共文件；不要连续 cherry-pick 多个都改过同一聚合页的提交后机械接受冲突两侧。
4. 每个 worktree 只提交本任务文件。使用路径限定的暂存命令核对清单，不得使用会把整个工作区变化一并加入的操作。
5. 分支合入前同步目标分支最新状态；发现公共资源键或类型名冲突时，在集成阶段统一改名并同步所有引用。

### 16.3 退化模式：多个 AI 共享同一工作目录

无法使用独立 worktree 时，必须遵守以下更严格的流程：

1. 每个 AI 开工时保存自己拟修改文件的清单和指纹，例如当前提交 ID 及文件内容哈希。
2. 工作 AI 只创建和修改控件私有文件，不碰公共热点；公共集成者是这些热点的唯一写入者。
3. 公共集成者一次只处理一个控件的集成清单。每次写入前重新读取文件，使用最小锚点补丁追加，不按旧行号修改，不替换整文件。
4. 页面条目、资源键和 `Generic.xaml` 合并行按现有分类与顺序插入；禁止借机全文件排序或格式化。
5. 如果某个私有文件在任务进行中被别人修改，原负责 AI 立即停止写入并报告冲突；不得根据记忆恢复“自己的版本”。
6. 工作 AI 不执行工作区级清理、回退、移动、批量重命名和提交操作。确需提交时，只能由协调者按已确认的路径清单处理。

### 16.4 每个控件必须提供的集成清单

控件实现完成后，即使尚未获得公共文件写权限，也必须交付以下清单：

```md
## CheemsXxx 集成清单
- 任务 ID / 基线提交：
- 新增私有文件：
- 修改私有文件：
- 控件类名与基类：
- Generic.xaml 合并行：
- Colors.xaml 新键（键 / 值 / 用途）：
- Brushes.xaml 新键（键 / 引用色键 / 用途）：
- CheemsKeys.cs 新常量：
- Demo 类别与最小 XAML 片段：
- Source pack URI：
- 搜索词（中英文、作者、类型、视觉特征）：
- 搜索 ViewModel 属性或元数据：
- App.xaml / MainWindow 变更：无（默认）/ 必要原因
- 已执行验证及结果：
- 尚未验证或已知差异：
```

清单中的代码片段必须以当前文件中的稳定元素为锚点，例如某一控件合并行或 `WrapPanel` 结束标签；不得只写“插入第 37 行”。

### 16.5 公共集成顺序

为减少 XAML 启动故障，公共集成者按以下顺序处理每个批次：

1. 仲裁控件类名、资源键、源码文件名和搜索标识。
2. 合并 `Colors.xaml`、`Brushes.xaml`、`Fonts.xaml` 与 `CheemsKeys.cs`，先检查键唯一性和引用方向。
3. 合并 `Generic.xaml`，确认 pack URI 的大小写、路径和实际文件完全一致。
4. 合并 Demo Source、页面条目和对应搜索元数据，保证 Visibility 绑定属性真实存在。
5. 必要时才修改 `App.xaml`、导航或共享基础设施；普通新增控件不应触碰这些文件。
6. 完成一次全量构建；若失败，只在当前集成批次范围内定位，不回退或覆盖其他任务。
7. 启动 Demo，逐项检查新控件及已有控件，确认异常日志无新增。

### 16.6 冲突处理规则

- **同名冲突**：在进入公共文件前解决。不得让两个类或资源键同名后依赖编译器报错仲裁。
- **同键同值**：可复用已有语义资源，但必须确认语义也相同；颜色数值相同不等于用途相同。
- **同键异值**：禁止后写覆盖前写。为控件创建独立语义键或由用户决定统一设计。
- **同一页面插入冲突**：保留双方控件条目，由页面集成者按现有分类整理；不得简单选择 ours/theirs 丢弃一侧。
- **基础设施冲突**：普通控件任务让位于专门的基础设施任务。需要新能力时先提交需求，由基础设施所有者实现后再接入。
- **视觉规则冲突**：用户针对某控件的明确要求优先；差异仅作用于该控件，不得未经授权改动全局规范或其他控件。

### 16.7 并行验收与最终交接

1. 工作 AI 至少保证私有 C# / XAML 文件语法有效，并报告因尚未公共注册而无法执行的验证，不能伪报“全量通过”。
2. 公共集成者在所有任务合入后执行最终 `dotnet build`、Demo 启动、异常日志增量检查和页面搜索检查。
3. 全量构建必须在所有公共文件集成完成后再作为最终结论；单个 worktree 的构建通过不能证明组合结果通过。
4. 验收发现问题时，按文件所有权退回对应任务。公共集成者只修接线问题，不擅自重写控件视觉；控件 AI 只修自己的私有实现，不越权修改聚合文件。
5. 最终交接必须列出：每个任务的私有文件、公共集成提交、构建结果、启动结果、未解决差异和是否仍有其他 AI 正在写入。
