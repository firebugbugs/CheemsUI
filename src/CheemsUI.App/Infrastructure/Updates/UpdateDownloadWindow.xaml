<Window x:Class="CheemsUI.App.Infrastructure.Updates.UpdateDownloadWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="下载更新"
        Width="510" Height="292"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="Transparent"
        AllowsTransparency="True">
    <Border Margin="8" Padding="28,25" Background="{Binding SurfaceBrush}" BorderBrush="{Binding BorderBrush}" BorderThickness="1" CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="26" ShadowDepth="8" Opacity="0.22" Color="#600A1624" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock Text="正在准备更新" FontSize="18" FontWeight="SemiBold" Foreground="{Binding PrimaryTextBrush}" />
                <TextBlock x:Name="PartVersion" Margin="0,5,0,0" FontSize="12" Foreground="{Binding SecondaryTextBrush}" />
            </StackPanel>
            <TextBlock Grid.Row="1" x:Name="PartStatus" Margin="0,27,0,9" FontSize="13" Foreground="{Binding PrimaryTextBrush}" />
            <ProgressBar Grid.Row="2" x:Name="PartProgress" Height="8" Minimum="0" Maximum="100" BorderThickness="0"
                         Background="{Binding ProgressTrackBrush}" Foreground="{Binding AccentBrush}" />
            <TextBlock Grid.Row="3" x:Name="PartDetail" Margin="0,9,0,0" FontSize="11" Foreground="{Binding SecondaryTextBrush}" />
            <Button Grid.Row="4" x:Name="PartCancelButton" Width="92" Height="34" HorizontalAlignment="Right"
                    Click="CancelButton_Click" Background="{Binding MutedSurfaceBrush}" BorderBrush="{Binding BorderBrush}" Foreground="{Binding PrimaryTextBrush}"
                    BorderThickness="1" Cursor="Hand" Content="取消下载" />
        </Grid>
    </Border>
</Window>
