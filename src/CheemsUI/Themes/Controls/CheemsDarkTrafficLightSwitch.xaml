<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CheemsUI">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Colors.xaml" />
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:CheemsDarkTrafficLightSwitch}">
        <Setter Property="Width" Value="266" />
        <Setter Property="Height" Value="104" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheemsDarkTrafficLightSwitch}">
                    <Viewbox Stretch="Uniform">
                        <Canvas x:Name="PartRoot" Width="266" Height="104">
                            <!-- .back: unlike the Praashoo7 variant, this source uses an 8px corner radius. -->
                            <Border Width="265.6" Height="104" CornerRadius="8"
                                    Background="{StaticResource Cheems.Brush.DarkTrafficLight.Back}">
                                <Border.Effect>
                                    <DropShadowEffect Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}"
                                                      Direction="270" ShadowDepth="7" BlurRadius="13" Opacity="0.3" />
                                </Border.Effect>
                            </Border>
                            <Border Width="265.6" Height="104" CornerRadius="8"
                                    Background="{StaticResource Cheems.Brush.DarkTrafficLight.Back}">
                                <Border.Effect>
                                    <DropShadowEffect Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}"
                                                      Direction="270" ShadowDepth="2" BlurRadius="2" Opacity="0.4" />
                                </Border.Effect>
                            </Border>
                            <Border Width="265.6" Height="103" CornerRadius="8"
                                    BorderThickness="0,0,0,1"
                                    BorderBrush="{StaticResource Cheems.Brush.DarkTrafficLight.Black}" />

                            <!-- .switch: inactive base is neutral Night Rider; only PartRedLight carries color. -->
                            <Canvas Canvas.Left="17.32" Canvas.Top="17.8" Width="70" Height="70">
                                <Ellipse Width="70" Height="70" Fill="{StaticResource Cheems.Brush.DarkTrafficLight.Black}">
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}"
                                                          ShadowDepth="0" BlurRadius="10" Opacity="0.2" />
                                    </Ellipse.Effect>
                                </Ellipse>
                                <Ellipse Width="68" Height="68" Margin="1"
                                         Fill="{StaticResource Cheems.Brush.DarkTrafficLight.Black}"
                                         Stroke="{StaticResource Cheems.Brush.DarkTrafficLight.NightRider}" StrokeThickness="5" />
                                <Grid Canvas.Left="7.5" Canvas.Top="7.5" Width="55" Height="55">
                                    <Ellipse Fill="{StaticResource Cheems.Brush.DarkTrafficLight.NightRider}" />
                                    <Ellipse x:Name="PartRedLight" Opacity="0">
                                        <Ellipse.Fill>
                                            <RadialGradientBrush>
                                                <GradientStop Offset="0" Color="{StaticResource Cheems.Color.DarkTrafficLight.GlowCenter}" />
                                                <GradientStop Offset="0.99" Color="{StaticResource Cheems.Color.DarkTrafficLight.Red}" />
                                                <GradientStop Offset="1" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                                <GradientStop Offset="1" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                            </RadialGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <Ellipse>
                                        <Ellipse.Fill>
                                            <DrawingBrush TileMode="Tile" Viewport="0,0,10,10" ViewportUnits="Absolute"
                                                          Viewbox="0,0,10,10" ViewboxUnits="Absolute">
                                                <DrawingBrush.Drawing>
                                                    <GeometryDrawing Geometry="M0,0 H10 V10 H0 Z">
                                                        <GeometryDrawing.Brush>
                                                            <RadialGradientBrush>
                                                                <GradientStop Offset="0.4243" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                                                <GradientStop Offset="0.9899" Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}" />
                                                            </RadialGradientBrush>
                                                        </GeometryDrawing.Brush>
                                                    </GeometryDrawing>
                                                </DrawingBrush.Drawing>
                                            </DrawingBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Grid>
                            </Canvas>

                            <!-- .switch1: yellow -->
                            <Canvas Canvas.Left="97.32" Canvas.Top="17.8" Width="70" Height="70">
                                <Ellipse Width="70" Height="70" Fill="{StaticResource Cheems.Brush.DarkTrafficLight.Black}">
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}"
                                                          ShadowDepth="0" BlurRadius="10" Opacity="0.2" />
                                    </Ellipse.Effect>
                                </Ellipse>
                                <Ellipse Width="68" Height="68" Margin="1"
                                         Fill="{StaticResource Cheems.Brush.DarkTrafficLight.Black}"
                                         Stroke="{StaticResource Cheems.Brush.DarkTrafficLight.NightRider}" StrokeThickness="5" />
                                <Grid Canvas.Left="7.5" Canvas.Top="7.5" Width="55" Height="55">
                                    <Ellipse Fill="{StaticResource Cheems.Brush.DarkTrafficLight.NightRider}" />
                                    <Ellipse x:Name="PartYellowLight" Opacity="0">
                                        <Ellipse.Fill>
                                            <RadialGradientBrush>
                                                <GradientStop Offset="0" Color="{StaticResource Cheems.Color.DarkTrafficLight.GlowCenter}" />
                                                <GradientStop Offset="0.99" Color="{StaticResource Cheems.Color.DarkTrafficLight.Yellow}" />
                                                <GradientStop Offset="1" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                                <GradientStop Offset="1" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                            </RadialGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <Ellipse>
                                        <Ellipse.Fill>
                                            <DrawingBrush TileMode="Tile" Viewport="0,0,10,10" ViewportUnits="Absolute"
                                                          Viewbox="0,0,10,10" ViewboxUnits="Absolute">
                                                <DrawingBrush.Drawing>
                                                    <GeometryDrawing Geometry="M0,0 H10 V10 H0 Z">
                                                        <GeometryDrawing.Brush>
                                                            <RadialGradientBrush>
                                                                <GradientStop Offset="0.4243" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                                                <GradientStop Offset="0.9899" Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}" />
                                                            </RadialGradientBrush>
                                                        </GeometryDrawing.Brush>
                                                    </GeometryDrawing>
                                                </DrawingBrush.Drawing>
                                            </DrawingBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Grid>
                            </Canvas>

                            <!-- .switch2: green; the last checked radio wins in the source group. -->
                            <Canvas Canvas.Left="177.32" Canvas.Top="17.8" Width="70" Height="70">
                                <Ellipse Width="70" Height="70" Fill="{StaticResource Cheems.Brush.DarkTrafficLight.Black}">
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}"
                                                          ShadowDepth="0" BlurRadius="10" Opacity="0.2" />
                                    </Ellipse.Effect>
                                </Ellipse>
                                <Ellipse Width="68" Height="68" Margin="1"
                                         Fill="{StaticResource Cheems.Brush.DarkTrafficLight.Black}"
                                         Stroke="{StaticResource Cheems.Brush.DarkTrafficLight.NightRider}" StrokeThickness="5" />
                                <Grid Canvas.Left="7.5" Canvas.Top="7.5" Width="55" Height="55">
                                    <Ellipse Fill="{StaticResource Cheems.Brush.DarkTrafficLight.NightRider}" />
                                    <Ellipse x:Name="PartGreenLight" Opacity="0">
                                        <Ellipse.Fill>
                                            <RadialGradientBrush>
                                                <GradientStop Offset="0" Color="{StaticResource Cheems.Color.DarkTrafficLight.GlowCenter}" />
                                                <GradientStop Offset="0.99" Color="{StaticResource Cheems.Color.DarkTrafficLight.Green}" />
                                                <GradientStop Offset="1" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                                <GradientStop Offset="1" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                            </RadialGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <Ellipse>
                                        <Ellipse.Fill>
                                            <DrawingBrush TileMode="Tile" Viewport="0,0,10,10" ViewportUnits="Absolute"
                                                          Viewbox="0,0,10,10" ViewboxUnits="Absolute">
                                                <DrawingBrush.Drawing>
                                                    <GeometryDrawing Geometry="M0,0 H10 V10 H0 Z">
                                                        <GeometryDrawing.Brush>
                                                            <RadialGradientBrush>
                                                                <GradientStop Offset="0.4243" Color="{StaticResource Cheems.Color.DarkTrafficLight.Transparent}" />
                                                                <GradientStop Offset="0.9899" Color="{StaticResource Cheems.Color.DarkTrafficLight.Black}" />
                                                            </RadialGradientBrush>
                                                        </GeometryDrawing.Brush>
                                                    </GeometryDrawing>
                                                </DrawingBrush.Drawing>
                                            </DrawingBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Grid>
                            </Canvas>
                        </Canvas>
                    </Viewbox>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PartRoot" Property="Opacity" Value="0.5" />
                            <Setter Property="Cursor" Value="No" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
