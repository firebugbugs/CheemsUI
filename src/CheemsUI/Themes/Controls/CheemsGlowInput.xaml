<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CheemsUI">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Colors.xaml" />
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--
      .input → CheemsGlowInput / PartSurface；box-shadow → PartGlow。
      Hover 与 Focus 使用同一连续状态进度，0.5s CSS ease 由控件代码求值。
    -->
    <Style TargetType="{x:Type local:CheemsGlowInput}">
        <Setter Property="Width" Value="240" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Padding" Value="12.8,0,0,0" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="{DynamicResource Cheems.Brush.GlowInput.Text}" />
        <Setter Property="CaretBrush" Value="{DynamicResource Cheems.Brush.GlowInput.Text}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheemsGlowInput}">
                    <Grid>
                        <!-- box-shadow: 0 0 0 7px rgb(74 157 236 / 20%). -->
                        <Border x:Name="PartGlow"
                                Margin="0" Opacity="0"
                                Background="{DynamicResource Cheems.Brush.GlowInput.Glow}"
                                CornerRadius="10"
                                IsHitTestVisible="False" />

                        <Border x:Name="PartSurface"
                                Background="{DynamicResource Cheems.Brush.GlowInput.Background}"
                                BorderBrush="{DynamicResource Cheems.Brush.GlowInput.TransparentBorder}"
                                BorderThickness="2"
                                CornerRadius="10"
                                ClipToBounds="True">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden" />
                                <TextBlock x:Name="PartPlaceholder"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           Text="{TemplateBinding Placeholder}"
                                           Foreground="{DynamicResource Cheems.Brush.GlowInput.Placeholder}"
                                           Visibility="Collapsed"
                                           IsHitTestVisible="False" />
                            </Grid>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
