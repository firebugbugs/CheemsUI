<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CheemsUI">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Colors.xaml" />
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:CheemsIosStretchSwitch}">
        <Setter Property="Width" Value="71" />
        <Setter Property="Height" Value="51" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheemsIosStretchSwitch}">
                    <!-- 51×31 本体居中，外围用于 CSS 两层 box-shadow。 -->
                    <Viewbox Stretch="Uniform">
                        <Canvas x:Name="PartRoot" Width="71" Height="51" Background="Transparent">
                            <Canvas Canvas.Left="10" Canvas.Top="7" Width="51" Height="31">
                                <Border Width="51" Height="31" CornerRadius="16"
                                        Background="{DynamicResource Cheems.Brush.IosStretchSwitch.Off}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="{StaticResource Cheems.Color.IosStretchSwitch.ShadowFar}"
                                                          BlurRadius="8" ShadowDepth="3" Direction="270" Opacity="0.15" />
                                    </Border.Effect>
                                </Border>
                                <Border Width="51" Height="31" CornerRadius="16"
                                        Background="{DynamicResource Cheems.Brush.IosStretchSwitch.Off}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="{StaticResource Cheems.Color.IosStretchSwitch.ShadowNear}"
                                                          BlurRadius="1" ShadowDepth="3" Direction="270" Opacity="0.06" />
                                    </Border.Effect>
                                </Border>
                                <Border x:Name="PartTrack" Width="51" Height="31" CornerRadius="16">
                                    <Border.Background>
                                        <SolidColorBrush Color="{StaticResource Cheems.Color.IosStretchSwitch.Off}" />
                                    </Border.Background>
                                </Border>

                                <!-- slider：默认 left=2，checked left=22；Host 同时承载阴影，避免不同步。 -->
                                <Grid x:Name="PartThumbHost" Canvas.Left="2" Canvas.Top="2"
                                      Width="27" Height="27">
                                    <Border CornerRadius="20"
                                            Background="{DynamicResource Cheems.Brush.IosStretchSwitch.Thumb}">
                                        <Border.Effect>
                                            <DropShadowEffect Color="{StaticResource Cheems.Color.IosStretchSwitch.ShadowFar}"
                                                              BlurRadius="8" ShadowDepth="3" Direction="270" Opacity="0.15" />
                                        </Border.Effect>
                                    </Border>
                                    <Border CornerRadius="20"
                                            Background="{DynamicResource Cheems.Brush.IosStretchSwitch.Thumb}">
                                        <Border.Effect>
                                            <DropShadowEffect Color="{StaticResource Cheems.Color.IosStretchSwitch.ShadowNear}"
                                                              BlurRadius="1" ShadowDepth="3" Direction="270" Opacity="0.06" />
                                        </Border.Effect>
                                    </Border>
                                    <Border CornerRadius="20"
                                            Background="{DynamicResource Cheems.Brush.IosStretchSwitch.Thumb}" />
                                </Grid>
                            </Canvas>
                        </Canvas>
                    </Viewbox>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="PartTrack"
                                                                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)">
                                            <SplineColorKeyFrame KeyTime="0:0:0.2"
                                                                 Value="{StaticResource Cheems.Color.IosStretchSwitch.On}"
                                                                 KeySpline="0,0,0.58,1" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="PartTrack"
                                                                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)">
                                            <SplineColorKeyFrame KeyTime="0:0:0.2"
                                                                 Value="{StaticResource Cheems.Color.IosStretchSwitch.Off}"
                                                                 KeySpline="0,0,0.58,1" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PartRoot" Property="Opacity" Value="0.5" />
                            <Setter Property="Cursor" Value="No" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
