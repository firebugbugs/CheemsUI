<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CheemsUI">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Colors.xaml" />
        <ResourceDictionary Source="/CheemsUI;component/Themes/Basic/Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--
      DOM 一一对应：
      PartBack=.loading svg polyline#back；PartFront=.loading svg polyline#front。
      原 DOM 没有交互状态；全部动画共用 1.4 秒周期，由控件代码按原关键帧和缓动逐帧求值。
    -->
    <Style TargetType="{x:Type local:CheemsPolylineLoader}">
        <Setter Property="Width" Value="64" />
        <Setter Property="Height" Value="48" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheemsPolylineLoader}">
                    <!-- SVG viewport 精确为 64×48，边界处描边按原版 viewport 裁切。 -->
                    <Viewbox Stretch="Uniform">
                        <Canvas Width="64" Height="48" ClipToBounds="True">
                            <!-- 背景 polyline#back -->
                            <Path x:Name="PartBack" StrokeThickness="3"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Stroke="{DynamicResource Cheems.Brush.PolylineLoader.Background}">
                                <Path.Data>
                                    <PathGeometry>
                                        <PathFigure StartPoint="0.157,23.954" IsClosed="False">
                                            <LineSegment Point="14,23.954" />
                                            <LineSegment Point="21.843,48" />
                                            <LineSegment Point="43,0" />
                                            <LineSegment Point="50,24" />
                                            <LineSegment Point="64,24" />
                                        </PathFigure>
                                    </PathGeometry>
                                </Path.Data>
                            </Path>

                            <!-- 前景 polyline#front -->
                            <Path x:Name="PartFront" StrokeThickness="3"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  StrokeDashCap="Round"
                                  StrokeLineJoin="Round"
                                  StrokeDashArray="16,48"
                                  StrokeDashOffset="64"
                                  Stroke="{DynamicResource Cheems.Brush.PolylineLoader.Foreground}">
                                <Path.Data>
                                    <PathGeometry>
                                        <PathFigure StartPoint="0.157,23.954" IsClosed="False">
                                            <LineSegment Point="14,23.954" />
                                            <LineSegment Point="21.843,48" />
                                            <LineSegment Point="43,0" />
                                            <LineSegment Point="50,24" />
                                            <LineSegment Point="64,24" />
                                        </PathFigure>
                                    </PathGeometry>
                                </Path.Data>
                            </Path>
                        </Canvas>
                    </Viewbox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
